<!--我发布的-->
<i-card wx:for="{{products}}" longpress="{{cardClick}}" wx:key="*this" title="{{item.name}}" longpress="{{cardClick}}"
        full="true">
    <view slot="content">创建时间：{{item.createTime}}</view>
</i-card>

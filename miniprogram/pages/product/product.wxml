<!--我发布的-->
<i-card wx:for="{{products}}" wx:key="*this" data-product="{{item}}" title="{{item.name}}" bindtap="cardClick"
        full="true">
    <view slot="content">创建时间：{{item.createTime}}</view>
</i-card>

<!--我买到的-->
<i-card wx:for="{{orders}}" wx:key="*this" data-order="{{item}}" title="{{item.productName}}" bindtap="cardClick"
        full="true">
    <view slot="content">创建时间：{{item.create_time}}</view>
</i-card>
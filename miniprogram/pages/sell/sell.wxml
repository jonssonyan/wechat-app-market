<!--我的卖出-->
<i-card wx:for="{{orders}}" wx:key="*this" data-product="{{item}}" title="{{product.name}}" bindtap="cardClick"
        full="true">
    <view slot="content">创建时间：{{item.create_time}}</view>
</i-card>
